
---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.7.0
  creationTimestamp: null
  name: completions.complete.yaml-copilot.com
spec:
  group: complete.yaml-copilot.com
  names:
    kind: Completion
    listKind: CompletionList
    plural: completions
    singular: completion
  scope: Namespaced
  versions:
  - name: v1alpha1
    schema:
      openAPIV3Schema:
        description: Completion is the Schema for the completions API
        properties:
          apiVersion:
            description: 'APIVersion defines the versioned schema of this representation
              of an object. Servers should convert recognized schemas to the latest
              internal value, and may reject unrecognized values. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources'
            type: string
          kind:
            description: 'Kind is a string value representing the REST resource this
              object represents. Servers may infer this from the endpoint the client
              submits requests to. Cannot be updated. In CamelCase. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds'
            type: string
          metadata:
            type: object
          spec:
            description: CompletionSpec defines the desired state of Completion
            properties:
              autoApply:
                default: false
                description: Wehether to apply the generated YAMLs automatically or
                  not
                type: boolean
              maxTokens:
                default: 256
                maximum: 4096
                minimum: 0
                type: integer
              userPrompt:
                description: Foo is an example field of Completion. Edit completion_types.go
                  to remove/update
                type: string
            required:
            - userPrompt
            type: object
          status:
            description: CompletionStatus defines the observed state of Completion
            properties:
              completion:
                description: 'INSERT ADDITIONAL STATUS FIELD - define observed state
                  of cluster Important: Run "make" to regenerate code after modifying
                  this file'
                items:
                  type: string
                type: array
              observedGeneration:
                format: int64
                type: integer
              resourceList:
                description: a list of the resources which were applied and are currently
                  in the cluster format in the type of namespace/resource.group/name
                type: string
            required:
            - completion
            - resourceList
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
status:
  acceptedNames:
    kind: ""
    plural: ""
  conditions: []
  storedVersions: []
